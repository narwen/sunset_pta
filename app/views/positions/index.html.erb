<% if @positions.count == 0 %>
  There are no positions
<% else %>
  <% @positions.each do |position| %>
    <br />
    <%= position.title %>
    <%= link_to "Delete #{position.title}", position, :method => :delete %>
  <% end %>
<% end %>